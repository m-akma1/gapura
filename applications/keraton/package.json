{
  "name": "@gapura/keraton",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "generate": "prisma generate",
    "migrate:deploy": "prisma migrate deploy",
    "migrate:dev": "prisma migrate dev",
    "dev": "node --watch dist/main.js",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@gapura/contracts": "workspace:*",
    "@gapura/crypto": "workspace:*",
    "@gapura/http-kit": "workspace:*",
    "@gapura/oauth-client": "workspace:*",
    "@prisma/adapter-pg": "7.9.1",
    "@prisma/client": "7.9.1"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "prisma": "7.9.1",
    "typescript": "^5.9.3"
  }
}
